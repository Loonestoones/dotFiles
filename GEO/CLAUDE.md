Read .cursor direcotry and its contents!

CRITICAL: Before working around any OPS/libops behaviour, first search the full OPS function
inventory in `.cursor/Logs/Log6-OPS-function-inventory.md` (every OPS function, CM4 vs CM7) —
the library almost certainly already has the function you need (lesson: UART_Task_StopPort).

